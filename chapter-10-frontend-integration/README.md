# Chapter 10 - Frontend Integration: Static, React, and WASM

Covers:

- Serving static assets from Rust
- API under `/api`
- Single-origin deployment shape
- Vite proxy example

Run:

```bash
cargo run
```

Open:

```text
http://127.0.0.1:3000
```

Dev proxy example is included in `frontend/vite.config.ts`.
